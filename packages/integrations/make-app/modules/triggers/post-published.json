{
	"label": "Post Published",
	"description": "Triggers when a post is successfully published to all platforms.",
	"type": "instant",
	"webhook": "relay-webhook",
	"parameters": [
		{
			"name": "events",
			"type": "hidden",
			"default": ["post.published"]
		}
	],
	"response": {
		"output": "{{body}}"
	}
}
