{
	"label": "Post Failed",
	"description": "Triggers when a post fails to publish.",
	"type": "instant",
	"webhook": "relay-webhook",
	"parameters": [
		{
			"name": "events",
			"type": "hidden",
			"default": ["post.failed"]
		}
	],
	"response": {
		"output": "{{body}}"
	}
}
