{
  "name": "zapier-relayapi",
  "version": "0.1.0",
  "description": "RelayAPI integration for Zapier — post to 21 platforms",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --testTimeout 10000",
    "zapier:push": "zapier-platform push",
    "zapier:validate": "zapier-platform validate"
  },
  "dependencies": {
    "zapier-platform-core": "19.0.0"
  },
  "devDependencies": {
    "zapier-platform-cli": "19.0.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "zapier": {
    "id": 0,
    "key": "relayapi"
  }
}
