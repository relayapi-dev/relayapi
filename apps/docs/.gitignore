out/
.open-next/

# API reference pages are generated from the pinned OpenAPI spec and committed.
# Only the generator's transactional working directories are build artifacts.
.api-reference-tmp/
.api-reference-backup/
