import { createDashboardRouteApp } from "../create-dashboard-route-app";
import { SettingsPage } from "../pages/settings-page";

export const SettingsRouteApp = createDashboardRouteApp(SettingsPage);
