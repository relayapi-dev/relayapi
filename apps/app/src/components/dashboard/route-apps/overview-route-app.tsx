import { OverviewPage } from "../pages/overview-page";
import { createDashboardRouteApp } from "../create-dashboard-route-app";

export const OverviewRouteApp = createDashboardRouteApp(OverviewPage);
