---
// Legacy /app/automation/new route — superseded by the in-place
// CreateAutomationDialog on the list page (Plan 2 — Unit B5, Task P4).
// We redirect so any old bookmarks land back on the list page, which opens
// the dialog automatically via `?new=1`.
return Astro.redirect("/app/automation?new=1", 307);
---
